<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happyhouse.QnA">

	<!-- 게시글 전체 조회 -->
	<select id="selectQnA" resultMap="qnaBasic">
		select qna_no, qna_title,
		qna_content, qna_userid, qna_datetime, reply_content, reply_datetime,
		reply_userid
		from qna_board
		order by qna_no desc
	</select>

	<!-- 게시글 상세 조회 -->
	<select id="selectQnAByNo" parameterType="int" resultMap="qnaBasic">
		select qna_no, qna_title, qna_content, qna_userid, qna_datetime,
		reply_content, reply_datetime, reply_userid
		from qna_board
		where qna_no = #{qna_no}
	</select>

	<!-- 게시글 등록 -->
	<insert id="insertQnA" parameterType="qna">
		insert into qna_board(
		qna_title, qna_content, qna_userid, reply_content, reply_userid
		) values (
		#{qna_title}, #{qna_content}, #{qna_userid}, #{reply_content}, #{reply_userid}
		)
	</insert>

	<!-- 게시글 수정 -->
	<update id="updateQnA" parameterType="qna">
		update qna_board
		set qna_title = #{qna_title},
		qna_content = #{qna_content},
		qna_userid = #{qna_userid},
		reply_content = #{reply_content},
		reply_userid = #{reply_userid},
		reply_datetime = now()
		where qna_no = #{qna_no}
	</update>

	<!-- 게시글 삭제 -->
	<delete id="deleteQnA" parameterType="int">
		delete
		from qna_board
		where qna_no = #{qna_no}
	</delete>

	<resultMap type="qna" id="qnaBasic">
		<result column="qna_no" property="qna_no" />
		<result column="qna_title" property="qna_title" />
		<result column="qna_content" property="qna_content" />
		<result column="qna_userid" property="qna_userid" />
		<result column="qna_datetime" property="qna_datetime" />
		<result column="reply_content" property="reply_content" />
		<result column="reply_datetime" property="reply_datetime" />
		<result column="reply_userid" property="reply_userid" />
	</resultMap>

</mapper>

